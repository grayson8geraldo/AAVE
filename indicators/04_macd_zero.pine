//@version=5
strategy("MACD Zero Cross [AAVE]", overlay=true, default_qty_type=strategy.percent_of_equity, default_qty_value=100, commission_type=strategy.commission.percent, commission_value=0.1, initial_capital=200)

f=input.int(12,"Fast"), s=input.int(26,"Slow"), sig=input.int(9,"Signal")
sl=input.float(5.0,"SL%")/100, tp=input.float(12.0,"TP%")/100

[macdLine, signalLine, hist] = ta.macd(close, f, s, sig)
long  = ta.crossover(hist, 0)
short = ta.crossunder(hist, 0)

if long
    strategy.entry("L", strategy.long)
    strategy.exit("LX","L", stop=close*(1-sl), limit=close*(1+tp))
if short
    strategy.entry("S", strategy.short)
    strategy.exit("SX","S", stop=close*(1+sl), limit=close*(1-tp))

plotshape(long, "Buy", shape.triangleup, location.belowbar, color.green, size=size.small)
plotshape(short, "Sell", shape.triangledown, location.abovebar, color.red, size=size.small)
